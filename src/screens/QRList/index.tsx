import { Text, View, SafeAreaView } from 'react-native';

import { SafeArea } from '../../components/SafeArea';

export const ListQR = () => {
  return (
    <SafeArea>
      <Text>LIST QR</Text>
    </SafeArea>
  );
};
