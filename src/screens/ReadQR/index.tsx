import { Text, View, SafeAreaView } from 'react-native';

import { SafeArea } from '../../components';

export const ReadQR = () => {
  return (
    <SafeArea>
      <Text>READ QR</Text>
    </SafeArea>
  );
};
